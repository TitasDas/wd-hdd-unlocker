# Safety Notes

- Back up important data before use.
- Connect only the intended WD locked drive while unlocking.
- Run with required privileges only when needed.
- If unlock/mount fails, stop and verify device identity before retrying.
- Review launcher logs for errors: `~/.local/state/wd-security/launcher.log`.
